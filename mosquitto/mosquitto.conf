listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type all

# The agent renders the actual Blynk bridge connection into this directory
# at runtime (see agent.py: render_bridge_conf) so the cloud auth token
# never has to live in an image layer.
include_dir /mosquitto/config/conf.d
